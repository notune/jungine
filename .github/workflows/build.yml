name: Build

on:
  push:
    branches: [glm-5]
  pull_request:
    branches: [glm-5]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        include:
          - os: ubuntu-latest
            artifact_name: jungle_engine
            asset_name: jungle_engine-linux
          - os: windows-latest
            artifact_name: jungle_engine.exe
            asset_name: jungle_engine-windows.exe

    steps:
      - uses: actions/checkout@v4

      - name: Setup MinGW (Windows)
        if: matrix.os == 'windows-latest'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: mingw-w64-x86_64-gcc mingw-w64-x86_64-make

      - name: Build (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          cd jungle_chess/engine
          make

      - name: Build (Windows)
        if: matrix.os == 'windows-latest'
        shell: msys2 {0}
        run: |
          cd jungle_chess/engine
          g++ -std=c++17 -O3 -Wall -Wextra -DNDEBUG -o jungle_engine.exe main.cpp
          ls -la

      - name: Test (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          cd jungle_chess/engine
          make test

      - name: Test (Windows)
        if: matrix.os == 'windows-latest'
        shell: msys2 {0}
        run: |
          cd jungle_chess/engine
          printf 'uci\nisready\nquit\n' | ./jungle_engine.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: jungle_chess/engine/${{ matrix.artifact_name }}
